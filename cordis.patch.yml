# 对话流渲染增强：思维链默认展开为可滚动子框 + 过程折叠为折叠栏。
# 纯 client UI 插件：host 壳不注入任何服务，也不下发配置——
# 全部配置在浏览器 设置 → 对话折叠 标签页内调整（localStorage 持久化）。
# 此补丁是插件的装载入口（把插件装配进 profile），不可省略。
- insert:
    - id: conversation-folding
      name: '@the-heart-fickle/dsh-conversation-folding'
